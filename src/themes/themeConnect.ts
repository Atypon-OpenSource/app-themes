import { Theme } from '../types'
import { theme } from './themeDefault'

export const themeConnect: Theme = {
  name: 'Connect',
  colors: theme.colors,
  font: theme.font,
  grid: theme.grid,
  shadow: theme.shadow,
}
